org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# NOTE: Implementation plan requires JDK 21, but only JDK 17 and JDK 26 are available
# Using JDK 17 for stability (JDK 26 is too new and may have compatibility issues)
# TODO: Install JDK 21 to meet implementation plan requirements
org.gradle.java.home=C:\\Program Files\\Java\\jdk-17
